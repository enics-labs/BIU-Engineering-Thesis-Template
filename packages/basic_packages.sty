\ProvidesPackage{common/basic_packages}


     %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
     %    Mandatory "everyday" packages      % 
     %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% The silence package lets me remove annoying warnings in the logfile
% -------------------------------------------------------------------
\usepackage{silence}
% The comment package creates the {comment} environment
% -----------------------------------------------------
\usepackage{comment}
% The color package lets us change font colors
% --------------------------------------------
\usepackage{color}
% The tikz package provides many tools for drawing in LaTeX
% ---------------------------------------------------------
\usepackage{tikz}
% Enable loren ipsum text
% -----------------------
\usepackage{lipsum}
% To enable Hebrew
% ----------------
%\usepackage[utf8]{inputenc}

     %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
     %    General pacakges heavily used in this framework     % 
     %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% The glossaries package allows building acronyms and an index
% --------------------------------------------------------------
\usepackage[acronym]{glossaries}

% The xspace package adds a space unless followed by punctuation
% --------------------------------------------------------------
\usepackage{xspace}
    \xspaceaddexceptions{-}
    \xspaceaddexceptions{$\times$}


     %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
     %  Spacing and Margins
     %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% The SETSPACE package provide things like 1.5 line spacing
% ---------------------------------------------------------
\usepackage{setspace}


% The GEOMETRY pakage sets up page margins
% ----------------------------------------
\usepackage[inner=2.5cm,outer=2.5cm,bottom=2.5cm]{geometry}
\onehalfspacing

   
% Spacing around Title/Authors:
% -----------------------------
% For IEEETran documents:
%   To reduce spacing after title block (below authors):
%      \IEEEoverridecommandlockouts
%      \IEEEaftertitletext{\vspace{-10px}}

% Spacing around Section Headers (Section, Subsection):
% -----------------------------------------------------
\usepackage{titlesec}
%%%%% USAGE:  
%%%%%   \titlespacing{command}{left spacing}{before spacing}{after spacing}[right]
%%%%%      the spacing is written {Xpt plus Ypt minus Zpt} 
%%%%%      where X is the spacing we want, 
%%%%%      Y is how much extra we’ll let LaTeX add and Z is how much less we’ll let it go.
% \titlespacing\section{0pt}{3pt plus 3pt minus 1pt}{3pt plus 3pt minus 1pt}
% \titlespacing\subsection{0pt}{3pt plus 3pt minus 1pt}{3pt plus 3pt minus 1pt}
% \titlespacing\subsubsection{0pt}{3pt plus 3pt minus 1pt}{3pt plus 3pt minus 1pt}

% Spacing around Figure Captions (Section, Subsection):
% -----------------------------------------------------
% The CAPTION package screws up some IEEETran defaults, so better use the built in lengths:
%   % space left between floats
%      \setlength{\floatsep}{3pt}
%   % space between last top float or first bottom float and the text
%      \setlength{\textfloatsep}{3pt}
%   % space above caption
%      \setlength{\abovecaptionskip}{-3pt plus 3pt minus 2pt}
%   % space below caption
%      \setlength{\belowcaptionskip}{0pt}
%   % Change Subfigure vertical spacing around subcaptions
%      \setlength{\subfigtopskip}{0pt}
%      \setlength{\subfigcapskip}{-2pt}
%      \setlength{\subfigbottomskip}{2pt}
%   % Change spacing around equations
%      \setlength{\abovedisplayskip}{-10pt}
%      \setlength{\belowdisplayskip}{-10pt}
   
% Change the spaces in the itemize
%---------------------------------
% \usepackage{enumitem}
    % \setlist{nolistsep}
    % \setlist[1]{itemsep=-3pt,topsep=3pt}


     %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
     %        Figures, Tables, Algorithms          %
     %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%----------------------------
%         Figures           %         
%----------------------------
% The graphicx package is the standard LaTeX graphics tool (though it's outdated)
% -------------------------------------------------------------------------------
\usepackage{graphicx}
% The subfigure package allows providing graphicx with subfigures
% ---------------------------------------------------------------
%\usepackage{subfigure}
\usepackage{subfig}
% The wrapfig package allows figures to be wrapped within text
% ---------------------------------------------------------------
% \usepackage{wrapfig} 
% The graphicspath option tells LaTeX the path to look for figures
% ---------------------------------------------------------------
    \graphicspath{{./Figures/}}

% The CAPTION package, has many options for figure captions, 
% but can change unwanted things, so prefer the TITLESEC package
% --------------------------------------------------------------
% \usepackage{caption}
	% Change spacing between captions and figure
    % ------------------------------------------
    %	\captionsetup{skip=2pt}
	%   \captionsetup{belowskip=-15pt}
	%   \captionsetup{aboveskip=2pt}
    % Change caption Font
    % -------------------
	%   \captionsetup{font={small,it}}


%----------------------------
%         Tables            %         
%----------------------------
% \usepackage{adjustbox}
% The MULTIROW package enables table cells that span multiple rows 
% ----------------------------------------------------------------
\usepackage{multirow}
% The ARRAY package enhances tables
% ---------------------------------
\usepackage{array}
    % This defines "P" for centering a sized column
    % ---------------------------------------------
    \newcolumntype{P}[1]{>{\centering\arraybackslash}p{#1}}
    % this adds some spacing between table rows
    % ---------------------------------------------
    % \renewcommand{\arraystretch}{1.2} 

%----------------------------
%        Algorithms         %
%----------------------------
% The ALGORITHM and ALGORITHMICX packages enable writing algorithms
% -----------------------------------------------------------------
\usepackage{algorithm}
\usepackage{algorithmicx}



     %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
     %           Bibliography            %
     %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\usepackage{cite}


    
     %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
     %        Math and symbols          %
     %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% The AMSMATH and AMSSYB packages are the basic packages for math
% ---------------------------------------------------------------
\usepackage{amsmath,amssymb,latexsym}
\usepackage{amsfonts}
\usepackage{mathtools}

% The UPGREEK pakage provides fonts for non-italic Greek letters
% ---------------------------------------------------------------
\usepackage{upgreek}

% The NICEFRAC pakage provides better inline fractions
% ----------------------------------------------------
\usepackage{nicefrac}

% The SIUNITX package provides standardized unit symbols
% ------------------------------------------------------
\usepackage{siunitx}



     %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
     %        Code Formatting           %
     %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% The LISTINGS package is for formatting source code
% --------------------------------------------------
\usepackage{listings}

% The MINTED package provides code highlighting
% ---------------------------------------------
\usepackage{minted}
    \setminted[python]{breaklines, framesep=2mm, fontsize=\footnotesize, numbersep=5pt}
    \setminted[matlab]{breaklines, framesep=2mm, fontsize=\footnotesize, numbersep=5pt}

     %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
     %        Header and Footer         %
     %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% The FANCYHDR package controls headers and footers
\usepackage{fancyhdr}
    \setlength{\headheight}{15pt}
    \fancyhf{}
    \renewcommand{\headrulewidth}{0pt} % prevents header underline
    \fancyhead[LE, LO]{\scriptsize \truncate{0.47 \textwidth} \leftmark}
    \fancyhead[RE, RO]{\scriptsize \truncate{0.47 \textwidth} \rightmark}
    \fancyfoot[C]{\thepage}

% The SECTSTY package controls the section style
% ----------------------------------------------
    \WarningFilter{latex}{Command \underbar has changed.}
    \WarningFilter{latex}{Command \underline has changed.}
\usepackage{sectsty}
    % This gets rid of some stupid warning due to the sectsty package
    \usepackage[T1]{fontenc}

% For fontsize options see see https://latex-tutorial.com/changing-font-size/
\chapternumberfont{\LARGE} 
\chaptertitlefont{\LARGE}  

% Without the TRUNCATE package, the PhD Thesis template breaks...
\usepackage[fit, breakall]{truncate}

     %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
     %       Specific Packages used in these Templates        %  
     %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
     
% The clipboard package enables Copy-Pasting for Reply to reviewers
% --------------------------------------------------------------
\usepackage{clipboard}
% The catchfilebetweentags package lets me copy a tagged section from an external file
% ------------------------------------------------------------------------------------
    \WarningFilter{etex}{Extended allocation already in use.}
\usepackage{catchfilebetweentags}
% Insert external pdf pages
% -------------------------
\usepackage[final]{pdfpages}


 

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%  Other packages that could be useful
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%\usepackage{etoolbox}
\usepackage[hidelinks]{hyperref}




